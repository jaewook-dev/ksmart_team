<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace가 인터페이스와 일치해야 연결된다 -->
<mapper namespace="com.smart.rider.contract.mapper.UnitMapper">

	<select id="getGoodsCodeMax" resultType="int">
		SELECT 
		MAX(CAST(substring(g_code,7) AS DECIMAL)) 
		AS maxcol 
		FROM tb_goods
	</select>
	<insert id="unitInsert" parameterType="com.smart.rider.contract.dto.UnitDTO">
		INSERT INTO contract_unit
	(contract_unit_code, member_id, contract_unit_month, contract_unit_oneyear, contract_unit_twoyear, contract_unit_date)
	VALUES ( #{memberId},
			 #{contractUnitMonth}, 0, 0, 0, NOW())
	</insert>


</mapper>